<%= form_with(model: standup, local: true) do |form| %>
  <% if standup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(standup.errors.count, "error") %> prohibited this standup from being saved:</h2>

      <ul>
        <% standup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :standup_date %>
    <%= form.date_select :standup_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
